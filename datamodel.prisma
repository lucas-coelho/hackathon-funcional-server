type Schedule {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  employee: String!
  beginDate: DateTime!
  endDate: DateTime!
  status: Status!
}

enum Status {
  Approved,
  Pending,
  Rejected,
}